name: Snapshot Build

on:
  workflow_dispatch:

  push:
    branches:
      - main

env:
  version_status: "beta"

jobs:
  build:
    uses: Crazy-Crew/.github/.github/workflows/build.yml@main

    with:
      branch: main

    secrets:
      webhook: ${{ secrets.CE_WEBHOOK }}
      modrinth_token: ${{ secrets.MODRINTH_TOKEN }}
      hangar_token: ${{ secrets.HANGAR_KEY }}