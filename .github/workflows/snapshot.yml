name: Snapshot Build

on:
  workflow_dispatch:

  push:
    branches:
      - dev

jobs:
  build:
    uses: Crazy-Crew/.github/.github/workflows/build.yml@main

    with:
      webhook: ${{ secrets.CE_WEBHOOK }}

    secrets: inherit