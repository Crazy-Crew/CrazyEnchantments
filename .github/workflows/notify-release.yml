name: Notify Release

on:
  workflow_dispatch:

jobs:
  build:
    uses: Crazy-Crew/.github/.github/workflows/notify.yml@main

    with:
      status: "release"
      branch: main

    secrets:
      webhook: ${{ secrets.BUILD_WEBHOOK }}